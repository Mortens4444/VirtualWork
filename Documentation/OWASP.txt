https://axen-cyber.com/hu/contact-2/

Implement 2FA (Two-factor authentication). ✗
The application cannot accept weak passwords. (Forbid to use weak passwords.) ✗
Implement Brute Force Attack protection. ✗
The application secure password restoration method (Do not send plain text passwords in e-mails). ✗
The application sessions can expire. (Invalidate sessions.) ✗
The application not sends Session ID in GET requests. (Server side, secure Session Manager implementation.) ✗

Secure password storage. (Safe password storage, hashed/encrypted storage.) ✓